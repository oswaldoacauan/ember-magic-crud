<h1>{{capitalMethodName}} {{capitalModelName}}</h1>

<form class="form">
  {{magic-form
    model=model
    errors=errors
    definitions=formDefinitionsMC
    submitted=submitted}}
  <button class="btn btn-primary" type="button" name="button" {{action 'saveRecord'}}>Save</button>
</form>

{{outlet}}
